const year = document.getElementById('year'); if (year) { year.textContent = new Date().getFullYear() }
const toggle = document.querySelector('.nav-toggle'); const nav = document.getElementById('nav'); if (toggle && nav) { toggle.addEventListener('click', () => { nav.classList.toggle('open') }) }
const form = document.getElementById('contactForm'); const note = document.getElementById('formNote'); if (form && note) { const mode = form.dataset.mode || 'local'; const params = new URLSearchParams(window.location.search); if (params.get('sent') === '1') { note.textContent = 'Thank you. We will contact you shortly.' } if (mode === 'remote') { form.addEventListener('submit', async (e) => { e.preventDefault(); try { const fd = new FormData(form); const data = {}; fd.forEach((v, k) => { data[k] = v }); const res = await fetch('https://formsubmit.co/ajax/hs1012006@gmail.com', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify(data) }); if (res.ok) { note.textContent = 'Thank you. We will contact you shortly.'; form.reset() } else { form.submit() } } catch (_) { form.submit() } }) } else { form.addEventListener('submit', (e) => { e.preventDefault(); note.textContent = 'Thank you. We will contact you shortly.'; form.reset() }) } }
const brandTiles = document.querySelectorAll('.brand-logo-tile'); const onIntersect = (entries) => { entries.forEach((entry) => { if (entry.isIntersecting) { entry.target.classList.add('animate') } }) }; if (brandTiles.length) { const obs = new IntersectionObserver(onIntersect, { root: null, threshold: .2 }); brandTiles.forEach((el, i) => { el.style.animationDelay = `${(i % 6) * .15}s`; obs.observe(el) }) }
